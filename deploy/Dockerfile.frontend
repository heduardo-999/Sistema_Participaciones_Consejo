# Dockerfile para Angular Frontend
FROM node:20-alpine

WORKDIR /app

COPY frontend_angular/package*.json ./
RUN npm install --legacy-peer-deps

COPY frontend_angular/ ./

EXPOSE 4200

CMD ["sh", "-c", "npx ng serve --host 0.0.0.0 --port 4200 --poll 2000"]
